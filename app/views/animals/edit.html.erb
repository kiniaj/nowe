<h1> Edytuj zwierza </h1>

<%= form_for(@animal) do |f| %>
  <% @animal.errors.full_messages.each do |err| %> 
    <li><%= err %></li> 
  <% end %>
  <%= f.label "Imie zwierzaka" %>
  <%= f.text_field :name %>
  <%= f.submit "Zapis mnie" %>
<% end %>